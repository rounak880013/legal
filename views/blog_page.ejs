<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="title" content="<%= blogPost.meta.title %>">
    <meta name="description" content="<%= blogPost.meta.description %>">
    <meta name="keywords" content="<%= blogPost.meta.keywords %>">
    <meta name="robots" content="<%= blogPost.meta.robots %>">
    <link rel="canonical" href="<%= blogPost.meta.canonicalUrl %>">
    <meta property="og:title" content="<%= blogPost.meta.ogTitle %>">
    <meta property="og:description" content="<%= blogPost.meta.ogDescription %>">
    <meta property="og:image" content="<%= blogPost.meta.ogImageUrl %>">
    <title><%= blogPost.title %></title>
    <style>
        /* Container for the main blog content */
        .blog-content {
            font-family: Arial, sans-serif;
            font-size: 1rem;
            line-height: 1.6;
            color: #333;
            margin-left: 10% ;
            margin-right: 10% ;
            margin-top: 10px ;
            margin-bottom: 10px ;
            padding: 10%;
            max-width: 80%; /* Sets a maximum width for better readability */
            background-color: #ffffff; /* Background for better contrast */
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1); /* Subtle shadow for elevation */
            border-radius: 8px;
        }
    
        /* Paragraph spacing */
        .blog-content p {
            margin: 1.5em 0; /* Space between paragraphs */
        }
    
        /* Heading styling */
        .blog-content h1, .blog-content h2, .blog-content h3 {
            color: #000000;
            font-weight: bold;
            margin-top: 1.5em;
            margin-bottom: 0.5em;
            line-height: 1.2;
        }
    
        /* Bold text styling */
        .blog-content b, .blog-content strong {
            color: #333;
            font-weight: 700;
        }
    
        /* Line breaks */
        .blog-content br {
            margin-bottom: 1rem;
            display: block;
            content: "";
        }
    
        /* Blockquote styling */
        .blog-content blockquote {
            margin: 1.5em 0;
            padding: 1em;
            background-color: #f8f9fa;
            border-left: 4px solid #007bff;
            font-style: italic;
            color: #555;
        }
    </style>
    
    
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <section class="contact-header"></section>
    <div class="blog-content">
        <h1><%- blogPost.h1 %></h1>
       <%- blogPost.content %>
    </div>

    <!-- Footer Section -->
    <%- include('partials/footer') %>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
